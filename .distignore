# Development and tooling — excluded from any distributed build.
.git
.github
.editorconfig
.gitignore
.distignore
.phpunit.result.cache

# Tests and CI config
tests/
phpcs.xml.dist
phpunit.xml.dist

# Composer dev dependencies and config
composer.json
composer.lock
vendor/

# Contributor / planning docs (internal)
docs/
README.md

# AI assistant + spec-kit tooling (never ships in a production plugin)
.claude/
.specify/
CLAUDE.md

# Build scripts (shell scripts trip Plugin Check's "application files" rule)
bin/

# Build artefacts
build/
dist/
*.zip
